[package]
name = "armature-validation"
version = "0.1.0"
edition = "2024"

[dependencies]
armature-core = { path = "../armature-core" }
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"
regex = "1.10"
once_cell = "1.19"
async-trait = "0.1"
tokio = { version = "1.35", features = ["full"] }

[dev-dependencies]

