# API Configuration
HOST=0.0.0.0
PORT=3000
ENVIRONMENT=development

# JWT Configuration
JWT_SECRET=change-me-in-production-use-a-long-random-string
JWT_EXPIRY_HOURS=24

# Database (optional)
# DATABASE_URL=postgres://postgres:postgres@localhost:5432/api

# Redis (optional)
# REDIS_URL=redis://localhost:6379

# Logging
RUST_LOG=info,api_full=debug
