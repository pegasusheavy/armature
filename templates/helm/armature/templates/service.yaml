apiVersion: v1
kind: Service
metadata:
  name: {{ include "armature.fullname" . }}
  labels:
    {{- include "armature.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "armature.selectorLabels" . | nindent 4 }}

